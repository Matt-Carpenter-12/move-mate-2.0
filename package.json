{
  "name": "move-mate-2.0",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "install": "cd server NODE_ENV=development && npm i && cd ../client NODE_ENV=development && npm i",
    "build": "cd client && npm run build",
    "render-build": "npm install && npm run build",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm run dev\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.3",
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "install": "^0.13.0",
    "nodemailer": "^6.9.14",
    "npm": "^10.8.2",
    "react-calendar": "^5.0.0",
    "uuid": "^10.0.0"
  }
}
